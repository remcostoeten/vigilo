import React from 'react'
import ReactDOM from 'react-dom/client'
import JiroApp from './jiro/app/jiro-app'

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <JiroApp />
  </React.StrictMode>,
)

